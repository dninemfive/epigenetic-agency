from gene import Gene

class Genome(object):
    def __init__(self, genes: set[Gene] = [], fitness: int = 0):
        self.genes: set[Gene] = genes
        self.fitness = fitness
        self.previous_state = None

    def complete_battle(self):
        self.fitness += 1

    def cross_with(self, other):
        new_genes: set[Gene] = set()
        ratio: float = float(self.fitness) / float(self.fitness + other.fitness)
        for gene in self.genes:
            pass

    def update_epigene(self, gene: Gene):
        pass